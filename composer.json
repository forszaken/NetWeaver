{
    "name": "forszaken/netweaver",
    "type": "project",
    "require": {
        "php": "^8.1"
    },
    "autoload-dev": {
        "psr-4": {
            "NetWeaver\\": "src/NetWeaver/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit --colors=always"
    }
}
